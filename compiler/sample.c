
int simple_var;
int var_with_value;

// this is a comment
int add_numbers(int a, int b) {
    int c = a + 2 * b;
    if (c > 3) {
        return 1;
    } else {
        return c;
    }
}

// this is another comment
void main() {
    // func1(5);
}

